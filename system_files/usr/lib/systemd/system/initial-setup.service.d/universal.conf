# Universal OS: run the first-boot account wizard only while the machine
# has no user account. Systems that already have users boot straight to login.
[Service]
ExecCondition=/usr/libexec/universal/needs-first-user
